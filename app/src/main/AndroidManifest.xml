<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Flixhub"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".Controller.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".Service.MessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <activity android:name=".Controller.GettingStartedActivity" android:noHistory="true" />
        <activity android:name=".Controller.LogChoiceActivity" />
        <activity android:name=".Controller.Register.RegisterActivity" />
        <activity android:name=".Controller.LoginActivity" />
        <activity android:name=".Controller.Register.RegisterInterestsActivity" />
        <activity android:name=".Controller.Register.RegisterProfileActivity" />
        <activity android:name=".Controller.Media.CategoryListActivity" />
        <activity android:name=".Controller.Home.HomeActivity" />
        <activity android:name=".Controller.Media.MediaActivity" />
        <activity android:name=".Controller.Player.PlayerActivity" />
    </application>

</manifest>